# AI Research Journal Papers

Private monorepo for preregistrations and reproducibility bundles produced by the AI Research Journal pipeline. Each accepted production run is stored under `papers/<run-id>/`.

The root-level preregistration files are retained as an auditable rejected preflight from the initial acceptance attempt; that proposal failed the novelty gate and no HPC experiment was run. They are not a published paper.
